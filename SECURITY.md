# 비밀정보와 분석 자료 관리

분석용 브라우저 프로필은 운영체제의 임시 디렉터리에 생성합니다.
쿠키, 세션, 로컬 저장소, 다운로드 메타데이터는 바이너리 로그에도 남을 수 있으므로
프로필 전체를 공개하지 않습니다. 재현에 필요한 자료는 민감정보를 제외한 별도 파일로 보존합니다.
다운로드한 브라우저·빌드 트리·의존성·웹 번들은 로컬에 유지하고 공개 추적 대상에서 제외합니다.

## 로컬 검사

Python 3.11 이상과 Gitleaks 8.30.1을 설치한 뒤 실행합니다.

```sh
python -m venv .venv
git config core.hooksPath .githooks
```

커밋 훅은 인덱스 전체의 브라우저 프로필·Google 키 패턴을 바이너리까지 검사하고,
Gitleaks로 스테이징된 변경의 다른 비밀정보를 검사합니다. 키 원문은 출력하지 않습니다.
GitHub Actions의 `Secret checks`는 현재 트리와 모든 신규 커밋을 검사합니다.
키를 중간 커밋에 추가했다가 삭제한 경우에도 차단합니다.
훅은 로컬에서 우회할 수 있으므로 원격 필수 검사와 Push protection을 함께 사용합니다.

전체 공개 기록을 감사할 때는 다음 명령을 사용하고 보고서는 저장소 밖에 보관합니다.

```sh
gitleaks git . --log-opts="--all" --redact=100 --ignore-gitleaks-allow --report-format json --report-path ../secrets.redacted.json
```

`.gitleaks.toml`의 예외는 Neon Skies 문제 설정의 명시적인 SHA-256 암호 해시 한 종류에만 적용합니다.
해당 파일의 다른 키나 다른 경로의 비밀정보는 예외가 아닙니다. 디렉터리 전체를 검사 예외로 두지 않습니다.
Git diff 검사만으로 바이너리·압축·Git LFS 원본·서브모듈 내부 전체가 검사됐다고 해석하지 않습니다.

## 과거 노출 대응

이전 공개 저장소의 Chrome 임시 프로필에서 Optimization Guide용 Google 키가 발견됐습니다.
새 공개 저장소는 해당 파일과 이전 커밋 기록을 포함하지 않습니다.
저장소 재생성은 공급자 측 키 폐기를 의미하지 않습니다. 소유권과 현재 키 상태는 확인이 필요합니다.
이전 사본이나 제3자의 다운로드까지 삭제됐다고 보장하지 않습니다.

과거 저장소의 브랜치·태그·미러를 새 저장소에 푸시하지 마세요.
기존 로컬 원본은 보관용으로 유지하고, 공개 작업은 새 `main` 계보에서 진행합니다.

새 노출 발견 시 키를 재공개하지 말고 경고·경로·커밋·발견 시각을 제한된 기록에 남깁니다.
자체 키라면 사용량을 확인하고 차단·폐기한 뒤 최소 권한의 새 키를 비밀 저장소에 배포합니다.
공급자 소유 키이면 공급자에게 대응을 요청합니다. 확인 증거 없이 폐기 완료로 처리하지 않습니다.
