const http=require('http'),puppeteer=require('puppeteer');let seen=[];
const b=http.createServer((q,r)=>{seen.push(['b',q.url,q.headers['sec-purpose'],q.headers['sec-fetch-site'],q.headers.cookie]);r.end(`<script>fetch('/ran')</script>`)});
const a=http.createServer((q,r)=>{seen.push(['a',q.url,q.headers['sec-purpose'],q.headers['sec-fetch-site'],q.headers.cookie]);if(q.url==='/rules'){r.setHeader('content-type','application/speculationrules+json');return r.end(JSON.stringify({prerender:[{source:'list',urls:['/bounce']}]}))}if(q.url==='/bounce'){r.setHeader('Supports-Loading-Mode','credentialed-prerender');return r.writeHead(302,{location:`http://localhost:${b.address().port}/review`}).end()}r.setHeader('Speculation-Rules','"/rules"');r.end('home')});
(async()=>{await Promise.all([new Promise(x=>a.listen(0,'127.0.0.1',x)),new Promise(x=>b.listen(0,'localhost',x))]);let br=await puppeteer.launch({executablePath:process.env.CHROME_PATH,headless:'new'}),p=await br.newPage();await p.setCookie({name:'sid',value:'x',url:`http://localhost:${b.address().port}`,sameSite:'Lax'});await p.goto(`http://127.0.0.1:${a.address().port}/`);await new Promise(x=>setTimeout(x,1500));console.log(seen);await br.close();a.close();b.close()})();
