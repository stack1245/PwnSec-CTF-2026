const http=require('node:http'),puppeteer=require('puppeteer');let seen=[];
const b=http.createServer((q,r)=>{let u=new URL(q.url,'http://localhost');if(u.pathname==='/target'){seen.push({url:q.url,site:q.headers['sec-fetch-site'],n:seen.length});r.setHeader('cache-control','no-store');r.statusCode=403;r.end('fixed')}else{let n=+u.pathname.slice(2);r.setHeader('cache-control','no-store');if(n<30)r.end(`<form action=/p${n+1}></form><script>document.forms[0].submit()</script>`);else r.end(`<script>setTimeout(()=>history.go(-31),100)</script>`)}});
const a=http.createServer((q,r)=>r.end(`<script>let w=open('http://localhost:${b.address().port}/target');setTimeout(()=>w.location='http://localhost:${b.address().port}/p0',300)</script>`));
(async()=>{await Promise.all([new Promise(x=>b.listen(0,'localhost',x)),new Promise(x=>a.listen(0,'127.0.0.1',x))]);let br=await puppeteer.launch({executablePath:process.env.CHROME_PATH,headless:'new'}),p=await br.newPage();await p.goto(`http://127.0.0.1:${a.address().port}/`);await new Promise(x=>setTimeout(x,5000));console.log(seen,await Promise.all((await br.pages()).map(async x=>[x.url(),await x.evaluate(()=>history.length)])));await br.close();a.close();b.close()})();
