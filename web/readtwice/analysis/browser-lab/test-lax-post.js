const http=require('http'),puppeteer=require('puppeteer');let seen=[];
const b=http.createServer((q,r)=>{seen.push({method:q.method,site:q.headers['sec-fetch-site'],cookie:q.headers.cookie});r.setHeader('content-type','text/html');r.end('ok')});
const a=http.createServer((q,r)=>{r.setHeader('content-type','text/html');r.end(`<form id=f method=post target=b action="http://localhost:${b.address().port}/x"><input name=x value=y></form><script>open('http://localhost:${b.address().port}/home','b');setTimeout(()=>f.submit(),300)</script>`)});
(async()=>{await Promise.all([new Promise(x=>a.listen(0,'127.0.0.1',x)),new Promise(x=>b.listen(0,'localhost',x))]);let br=await puppeteer.launch({executablePath:process.env.CHROME_PATH,headless:'new'}),p=await br.newPage();p.on('console',x=>console.log(x.text()));await p.setCookie({name:'sid',value:'x',url:`http://localhost:${b.address().port}`,sameSite:'Lax'});await p.goto(`http://127.0.0.1:${a.address().port}/`);await new Promise(x=>setTimeout(x,1000));console.log(seen,(await br.pages()).map(x=>x.url()));await br.close();a.close();b.close()})();
