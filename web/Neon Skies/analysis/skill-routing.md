# Skill routing

- Primary: ctf-web
- Evidence: The official category is web; the provided source contains an HTTP application, nginx middleware, and an automated browser bot.
- Roles: ctf-solver owns the canonical workspace, solver, flag, write-ups, and validation; ctf-web guides application mapping and exploit validation.
- Re-evaluate when: the decisive blocker becomes native memory corruption, pure cryptography, or standalone artifact recovery.
